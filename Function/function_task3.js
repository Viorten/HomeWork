/*Функція min(a, b) (Задача)
важливість: 1
Напишіть функцію min(a, b), яка повертає менше з двох чисел a та b.

Наприклад:
min(2, 5) == 2
min(3, -1) == -1
min(1, 1) == 1*/

/* - - - Моя відповідь - - - */
function min (a,b) {
    if (a < b) {
    return a;
 }   else {
    return b;
 }
}
